# Stow commands

Install the Windows PowerShell profile package from Cygwin:

```bash
cd '/home/jdamon/Projects/dotfiles'
stow --target='/cygdrive/c/Users/jdamon' windows-powershell
```

The same command is wrapped by [`install.bat`](install.bat) for Windows.
